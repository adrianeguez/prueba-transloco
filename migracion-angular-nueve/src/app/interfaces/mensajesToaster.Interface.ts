import {ToasterInterface} from './toaster.interface';

export interface MensajesToasterInterface {
  ToasterInterface?: ToasterInterface;
  toastExitoEditar?: ToasterInterface;
  toastExitoEliminar?: ToasterInterface;
  toastErrorCrear?: ToasterInterface;
  toastErrorEditar?: ToasterInterface;
  toastErrorEliminar?: ToasterInterface;
  toastExitoEstado?: ToasterInterface;
  toastErrorEstado?: ToasterInterface;
  toastErrorConexionServidor?: ToasterInterface;
  toastErrorMostrar?: ToasterInterface;
  toastExitoReset?: ToasterInterface;
  toastErrorCrearCampoRequerido?: ToasterInterface;
  toastErrorEditarCampoRequerido?: ToasterInterface;
  toastExitoEliminarVacio?: ToasterInterface;
  toastExitoCrearVacio?: ToasterInterface;
  toastExitoEditarVacio?: ToasterInterface;
  toastErrorCrearVacio?: ToasterInterface;
  toastErrorEditarVacio?: ToasterInterface;
  toastErrorEliminarVacio?: ToasterInterface;
  toastExitoEstadoVacio?: ToasterInterface;
  toastErrorEstadoVacio?: ToasterInterface;
  toastErrorConexionServidorVacio?: ToasterInterface;
  toastErrorMostrarVacio?: ToasterInterface;
  toastErrorReset?: ToasterInterface;
  ToastErrorTrayendoDatos?: ToasterInterface;
  toastExitoCargarDatos?: ToasterInterface;
  toastErrorCargarDatos?: ToasterInterface;
  toastExitoOrdenVacio?: ToasterInterface;
  toastErrorOrdenVacio?: ToasterInterface;
  toastExitoReverso?: ToasterInterface;
  toastExitoPagar?: ToasterInterface;
  toastErrorPagar?: ToasterInterface;
  toastExitoTarjeta?: ToasterInterface;
  toastErrorServicio?: ToasterInterface;
}
